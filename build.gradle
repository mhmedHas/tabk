// Top-level build file

buildscript {
    repositories {
        google()
        mavenCentral()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:8.2.2'
        // لإضافة Firebase للمشروع (Google Services plugin)
        classpath 'com.google.gms:google-services:4.4.2'
    }
}

